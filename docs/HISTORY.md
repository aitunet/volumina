# HISTORY — Volumina

> Append-only. Never edit or delete an earlier entry.
> One line per completed task, written the moment it passes verification.
> Architectural choices are recorded here with the `DECISIÓN:` prefix and
> expanded in `docs/decisions.md`.

---

## 2026-09

- **2026-09-03** — Repository normalised before any code. `volumina-CLAUDE.md` renamed to `CLAUDE.md`, `volumina-PROGRESS.md` renamed to `docs/STATE.md`, `volumina-pro-CLAUDE.md` moved out of this repository (it belongs to `volumina-pro`).
- **2026-09-03** — **DECISIÓN:** the project adopts the global TUNET state convention. State lives in `docs/STATE.md` (overwritten), the work log in `docs/HISTORY.md` (append-only). No `PROGRESS.md` and no `docs/session-log.md` exist anywhere in the project. See `docs/decisions.md`.
- **2026-09-03** — S0 boot loop fixed: creating `docs/` is now the first task of S0, so every later task has somewhere to be recorded. S0 file scope rewritten to cover everything S0 creates. Axe and block/classic theme checks in the definition of done conditioned to tasks that produce visible front-end output. "Guideline 5" replaced by its content: the directory's Detailed Plugin Guidelines forbid functionality that is locked and unlocked by paying, so the free plugin's Pro screen is purely presentational.
- **2026-09-03** — S0.1 done: `docs/` created with `STATE.md`, `HISTORY.md`, `decisions.md`, `backlog.md` and `api.md`. `decisions.md` seeded with the six choices already settled in `CLAUDE.md` (state convention, progress table, chapters as CPT, integer seconds, Pro as an external consumer of the public API, `src/Support/` extractable). `api.md` created as an explicit not-yet-stable stub; it is written for real in S4.
- **2026-09-03** — S0.2 done: repository initialised on `main` with `user.email = ai@tunetdesign.com`, remote `origin` set to `github.com/aitunet/volumina`, plus `.gitignore` and `.editorconfig` (tabs for PHP/JS/CSS, spaces for YAML/JSON/Markdown, per WordPress standards).
- **2026-09-03** — S0.2 addendum: `.gitattributes` added with `* text=auto eol=lf` and `core.autocrlf` disabled locally, so Windows checkouts keep the LF endings that `.editorconfig` and PHPCS expect.
- **2026-09-03** — Two commits sit on local `main` (`8cac19a`, `299f8e0`). The push to `github.com/aitunet/volumina` is pending: the remote is reachable and empty, but Git Credential Manager needs an interactive GitHub login. Nothing is lost — the commits are the save point.
- **2026-09-03** — Push unblocked and done: `main` is on `github.com/aitunet/volumina` at `39c8313`. The cause was the remote URL lacking the user, so Git Credential Manager looked up `github.com` with no username, found nothing and opened a GUI. Setting `origin` to `https://aitunet@github.com/...`, the form every other TUNET repo already uses, hit the stored credential and pushed without any prompt.
- **2026-09-03** — S0.3 written, not verified: `composer.json` with PSR-4 `TUNET\Volumina\ -> src/`, dev dependencies (WPCS 3, PHPCompatibilityWP, PHPStan + phpstan-wordpress, PHPUnit Polyfills) and a `lint` script running PHPCS then PHPStan, as the definition of done requires. JSON validated with node. **This machine has no PHP, no Composer and no Docker**, so `composer validate`, `composer lint` and `wp-env start` cannot run here and no `composer.lock` exists yet. Recorded as a blocker in `docs/STATE.md`.
- **2026-09-03** — S0.4 done and verified: `package.json` with `@wordpress/scripts` 34 and `@wordpress/env` 11, plus build, start, format, lint and env scripts. `npm run lint:pkg-json` runs clean; it caught three real faults first (property order, missing `repository`, missing `bugs`), all fixed. `package-lock.json` committed.
- **2026-09-03** — S0.5 to S0.7 written: `volumina.php` (header, `ABSPATH` guard, `strict_types`, namespaced `VERSION` and `PLUGIN_FILE`, and a Composer autoloader require that returns quietly instead of fatalling when `vendor/` is absent), `uninstall.php` (guarded by `WP_UNINSTALL_PLUGIN`, with the opt-in gate spelled out and nothing to delete until S1 and S5 exist) and a `readme.txt` skeleton whose short description is 118 characters and whose FAQ states plainly that files are not protected.
- **2026-09-03** — S0.8 to S0.11 written: `.wp-env.json` (latest core, this plugin, `WP_DEBUG` and `SCRIPT_DEBUG` on), `phpcs.xml` (WordPress ruleset minus `Files.FileName` because names follow PSR-4, text domain `volumina`, prefixes `volumina` and `TUNET\Volumina`, `minimum_wp_version` 6.6, `testVersion` 8.1-, plus PHPCompatibilityWP), `phpstan.neon` at level 6 over `src`, `volumina.php` and `uninstall.php`, and `.github/workflows/ci.yml` with a PHP job (composer validate, PHPCS, PHPStan) and a Node job. `src/.gitkeep` exists so PHPStan has the directory from the start instead of erroring until S1. CI uses `composer update` because no `composer.lock` can be generated on this machine; it switches to `composer install` when the lock is committed. The JS lint and build steps and the PHPUnit job join in S3 and S1 respectively, rather than running over an empty tree.
- **2026-09-03** — S0.12 written: i18n wiring. `volumina.php` loads the `volumina` text domain from `/languages` on `init` — not earlier, since WordPress 6.7 emits a notice for early loading — and `languages/` exists from the start. The `.pot` generator is the `make-pot` npm script, running WP-CLI inside wp-env; it cannot be executed here for want of Docker, so `languages/volumina.pot` does not exist yet.
- **2026-09-03** — CI green twice, on `3b1d17a` and on `6b6b8b3`. Every step passed: `composer validate --strict`, dependency install, PHPCS against the WordPress standard, PHPStan level 6, `npm ci` and `lint:pkg-json`. That is real verification for eleven of the twelve S0 tasks, `volumina.php` and `uninstall.php` included.
- **2026-09-03** — S0 stops one task short. `wp-env start` has never run because there is no Docker on this machine, so "plugin active, zero PHP notices" is unproven and S0 stays open. Everything else in the slice is written, committed, pushed and green.
- **2026-09-03** — Local PHP toolchain installed without administrator rights: portable PHP 8.3.33 NTS under `C:\Users\mage2\tools\php83` (own `php.ini` enabling openssl, mbstring, curl, fileinfo and zip) and `composer.phar` 2.10.3 in `C:\Users\mage2\tools`. Nothing was added to PATH and nothing was installed system-wide; both are deletable directories. Docker was **not** installed: it needs administrator rights and WSL, neither of which exists here, and forcing it would mean throwing UAC dialogs at the user.
- **2026-09-03** — `composer.lock` generated and committed, and `config.platform.php` pinned to `8.1` so the lock resolves against the version the plugin declares rather than the 8.3 that happens to be installed locally. Resolved set: WPCS 3.4.1, PHPCompatibilityWP, PHPStan with `php-stubs/wordpress-stubs` v7.1.0, PHPUnit 9.6.36. No security advisories. CI now runs `composer install` and `composer validate --strict` without `--no-check-lock`, so the lock is checked for drift on every push.
- **2026-09-03** — `composer lint` verified locally for the first time: PHPCS clean over `volumina.php` and `uninstall.php` in 1.45 s, PHPStan level 6 **no errors**. Note for this machine: PHPStan hangs when it forks its parallel worker — two idle processes, no progress — and `parallel.maximumNumberOfProcesses: 1` does not help because a worker is still forked. Running `php vendor/bin/phpstan analyse --debug` is genuinely single-process and works. This is a local firewall or antivirus quirk, not a project problem: PHPStan runs in seconds on CI, so `phpstan.neon` was left clean rather than patched around it.
- **2026-09-03** — Docker replaced rather than waited for. A local WordPress 7.1 now runs on portable PHP 8.3 with the official SQLite database integration drop-in, at `C:\Users\mage2\tools\wp-site`, with the repository attached by a directory junction; WP-CLI 2.12 drives it. Nothing of this lives in the repository. See `docs/decisions.md`.
- **2026-09-03** — S0 runtime verification passed: `wp plugin activate volumina` succeeds, `wp plugin list` shows it active at 0.1.0, `init` fires, the namespaced `VERSION` constant and the Composer autoloader resolve inside WordPress, and a full front-end render of the home page produces 72 KB of HTML with an empty `debug.log`. Zero PHP notices, twice over.
- **2026-09-03** — S0 complete, 12 of 12. `languages/volumina.pot` generated with WP-CLI, headers only for now. `readme.txt` moved to `Tested up to: 7.1`, which is now a true statement rather than an assumption: the plugin has actually been run on 7.1. `Requires at least` stays 6.6, as `CLAUDE.md` specifies.
- **2026-09-03** — S1.1 done and verified in a running WordPress: `volumina_book` registered, public, in REST, archive at `/audiobooks`, singles at `/audiobook/{slug}`, cover labels renamed from "featured image" so the editor speaks about books rather than posts. Six meta keys — narrator, publisher, isbn, language, total_duration, cover_id — all prefixed `volumina_`, all `show_in_rest`, integers sanitised with `absint` and strings with `sanitize_text_field`, each guarded by an `auth_callback` that defers to `edit_post` on the specific post rather than a blanket capability. A book created with WP-CLI keeps its meta on the way back out. PHPCS clean, PHPStan level 6 clean, `debug.log` empty.
- **2026-09-03** — Two pieces of scaffolding landed with it: `src/Support/Registrable.php`, an interface that knows nothing about audiobooks and is meant to leave with the rest of `Support/` into the next TUNET plugin, and `src/Plugin.php`, which does nothing but hold the component list and call `register()` on each. `volumina.php` gained one line, `Plugin::boot()`, and stays a bootstrap.
- **2026-09-03** — S1.2 done and verified: `volumina_chapter` registered with `volumina_book_id`, `volumina_attachment_id`, `volumina_duration` and `volumina_order`. Deliberately not public and not publicly queryable — a chapter has no front end of its own, it is reached through its book — but `show_in_rest` is on, because the player will need it. It sits in the admin under the Audiobooks menu rather than getting one of its own. Proof: three chapters created in the order 3, 1, 2 come back from `WP_Query` as 1, 2, 3. PHPCS, PHPStan level 6 and an empty `debug.log`.
- **2026-09-03** — S1.3 done and verified: `volumina_genre` and `volumina_series`, both hierarchical, both on the book, both in REST and shown as admin columns. A term of each assigned to a book and read back.
- **2026-09-03** — S1.4 done and verified: `{prefix}volumina_progress` created through `dbDelta`, with `Migrator` gating it on the `volumina_db_version` option. **DECISIÓN:** the table has no surrogate key — `(user_id, book_id)` is the primary key, which is the uniqueness constraint the data model asks for, and a duplicate row becomes impossible rather than merely unwanted. The migration check runs on `plugins_loaded`, not only on activation, because plugins are usually updated by replacing files without ever being deactivated; the cost is one read of an autoloaded option. Proof: deleting the version option and firing `plugins_loaded` recreates the schema, and two writes for the same listener and book leave exactly one row, updated rather than duplicated.
- **2026-09-03** — S1.5 done and verified: an "Audiobook details" meta box on the book screen. It does not repeat the field list; it iterates `Book::meta_keys()`, so a meta key cannot exist in the schema and be quietly missing from the form. The cover is picked from the media library through the frame WordPress already ships, driven by a small vanilla script with no jQuery and no build step. Proof, all in one run: the box renders with its six fields and a nonce; a save carrying a forged nonce leaves the meta untouched; a real save turns `  Marc Vigil <script>alert(1)</script>  ` into `Marc Vigil`, `-42` into `42` and `19abc` into `19`. Empty `debug.log`.
- **2026-09-03** — **DECISIÓN:** the book has one cover, not two. `thumbnail` support was removed from `volumina_book` and the featured-image labels with it, because the data model names `volumina_cover_id` and having both would give an editor two places to set the same thing and the plugin two sources of truth to disagree about. Noted in `docs/backlog.md`: if theme or block integration later wants a real featured image, mirroring `volumina_cover_id` into `_thumbnail_id` is the follow-up, and it is a schema decision.
- **2026-09-03** — Worth remembering: `get_post_meta()` returns stored strings even for meta registered as `integer`; only REST coerces the type. Every consumer casts with `(int)`. Seen while verifying that `absint` had done its job — the value was right, the type was `string`.
- **2026-09-05** — S1.6 done and verified: the chapter list on the book screen. Chapters render in order with their running time, each row links to its own editor, and the order is saved the moment it changes — by drag for a mouse, by Up and Down buttons for everyone else, because an ordering tool that needs a mouse is an ordering tool some people cannot use at all. `Support/Duration` formats seconds as `h:mm:ss` or `m:ss` and knows nothing about audiobooks, so it leaves with the rest of `Support/`. Proof, in a running WordPress: a forged nonce dies with `-1` and writes nothing; a subscriber holding a valid nonce gets a 403 and writes nothing; an editor reordering four chapters saves four rows; and a chapter belonging to another book, plus a book post smuggled into the same payload, are both skipped without consuming a position. PHPCS, PHPStan level 6, `npm run lint` (js, css and pkg-json) all clean, `debug.log` still absent.
- **2026-09-05** — Two faults found in that work before it was committed, both caught by verification rather than by reading. First: `Chapter::for_book()` ordered by the `volumina_order` meta inside `WP_Query`, which joins on that key, so a chapter that had never been given a position had no row to join to and vanished from its own book without a sound. The sort now happens in PHP over the meta cache `WP_Query` has already primed — a book has tens of chapters, so it costs nothing and cannot lose one. Position zero is treated as "not placed yet" and sorts last, where an editor will find it, rather than first pretending to be chapter one. Second: the two failure messages in `admin-chapters.js` were hard-coded English. They now come from `wp_localize_script`, along with a "Moved to position %1$d of %2$d." announcement, so keyboard reordering says where the chapter landed instead of moving it in silence.
- **2026-09-05** — Also fixed while verifying: `save()` cleared the `aria-live` region a fraction of a second after the keyboard move had announced the new position, erasing it before a screen reader could read it. The clear moved into `announce()` itself, which now empties the region and sets the message on the next frame — that keeps the same message audible twice in a row, which was the only thing the clear was ever for.
- **2026-09-05** — `languages/volumina.pot` regenerated with WP-CLI: 109 strings, translator comments carried through on every placeholder.
- **2026-09-05** — S1.7 done and verified: the chapter details box. A chapter picks its book from a select, its audio from a media frame restricted to audio, and its running time fills itself in from the file. The position is shown but not editable, with a link to the book screen — two screens that both set the position would be two sources of truth waiting to disagree, the same reasoning that gave the book one cover instead of two. `Chapter::next_order()` counts from the highest position in use rather than from the number of chapters, so a book whose numbering has gaps cannot hand the same position to two chapters.
- **2026-09-05** — Nine cases proved in a running WordPress, one save each: a forged nonce writes nothing; a logged-out request with a valid nonce writes nothing; audio chosen with the duration left blank reads 2730 seconds out of the file and lands at position 5, the end of its book; a duration corrected by hand survives the next save; an image attachment smuggled into the audio field is rejected on the server by its MIME type, not by the browser; an attachment ID that does not exist is rejected; moving the chapter to another book renumbers it to the end of that one; a book ID that is not a book clears the book and the position together; and saving twice without changing anything leaves the position where it was instead of pushing it along. `debug.log` still absent.
- **2026-09-05** — Worth remembering: `WordPress.Security.EscapeOutput` reads the expression at the `printf` call site, not the declared parameter type, so an `int` argument still needs its `(int)` cast written out there. Three of them in `ChapterMetaBox`; `BookMetaBox` had already been doing it.
- **2026-09-05** — Two limits written into `docs/backlog.md` rather than fixed: the book select lists every book, which is right for tens and wrong for hundreds (the replacement is a search-backed control, never a truncated list), and chapter positions only stay contiguous because the reorder endpoint renumbers the whole book, so deleting a chapter leaves a gap until the next drag. Harmless — order is compared, never counted on to be dense. `.pot` regenerated: 128 strings.
- **2026-09-05** — S1.8 done and verified, and with it **S1 is complete**. A single book now renders its cover, its details and its chapters in order on the front end. The details leave out what is empty rather than printing a blank row, and the running time falls back to the sum of the chapters when the book states none — which is exactly what the book screen already promised the editor. Durations carry a machine-readable `datetime`, so `Support\Duration` gained an `iso8601()` alongside `format()`.
- **2026-09-05** — **DECISIÓN:** the minimal front end renders through `the_content`, not through a template file. A template has to have an opinion about the page around it, and block themes and classic themes disagree about what that page is; a content filter has none, so the same code lands correctly inside whatever wrapper the theme provides. `src/Frontend/` joins the architecture. Nothing in it is filterable or overridable on purpose: the public API is written in S4, and an extension point invented early is a promise made by accident — one the S3 blocks would have to break. See `docs/decisions.md`.
- **2026-09-05** — Verified in Twenty Twenty-Five (block) and Twenty Twenty-One (classic), at 390px and 1280px, with axe-core against WCAG 2.1 AA over the plugin's own output: zero violations, zero incomplete, no horizontal overflow inside the block. The stylesheet declares no text colour at all, so every pair inherits the theme's palette and its contrast along with it; that is why one file suits both theme families.
- **2026-09-05** — Two defects the automated checks did not catch and a screenshot did. The chapter list had lost its numbers: `display: flex` on an `<li>` costs it its list marker, and a chapter list with no numbers is not a chapter list. The flex row moved to a span inside the item. And the details list was ragged, each pair finding its own margin; it is now one grid for the whole `<dl>`, with the wrapper divs set to `display: contents`, so the labels line up in a column. Lesson worth keeping: axe passing is not the same as the thing looking right.
- **2026-09-05** — Test infrastructure landed, as S0 promised it would in S1. `phpunit.xml.dist`, `tests/php/bootstrap.php` and 20 tests over `Support\Duration`, 26 assertions, including a property that the readable form and the ISO form always describe the same number of seconds. **DECISIÓN:** the unit suite loads no WordPress. The WordPress test suite needs a database server and, in practice, the `wp-env` this machine cannot run; waiting for it would have meant shipping S1 with no tests at all, while the code that does call WordPress is exercised against a real running install — stronger evidence than a mock of the thing under test. Coverage is therefore honest but narrow, and that is written down rather than glossed over.
- **2026-09-05** — CI caught up with reality: the PHPUnit job is real now, and `lint:js` and `lint:css` were switched on. They had been deferred to S3 on the grounds that the source tree was empty; it is not empty any more, and both were already passing locally. `.pot` regenerated: 131 strings.
- **2026-09-05** — Two things fixed in the local scratch WordPress, neither of them plugin code: the permalink structure had been corrupted to `/C:/Program Files/Git/%postname%/` by Git Bash path conversion in an earlier session, which would have made every front-end URL unreachable; and `DISABLE_WP_CRON` was set, because a cron loopback deadlocks PHP's single-threaded built-in server. Worth remembering: pass WP-CLI arguments that begin with a slash from PowerShell, not from Git Bash. The built-in server wedges under a browser's keep-alive connections regardless, so the front end is rendered in process with `wp eval-file` and `template-loader.php` and the resulting HTML is read from disk — the markup and every stylesheet are the real ones.
- **2026-09-05** — S2.1 done and verified: the audio streaming endpoint. Range requests are not a nicety here — without them a listener cannot seek, a browser will not scrub, and iOS refuses to play at all — so the endpoint supports single ranges, open-ended ranges, suffix ranges, `HEAD`, `ETag`/`If-None-Match`, `If-Modified-Since` and `If-Range`. The chapter ID in the URL is treated as a question, never an answer: it is resolved to a real chapter, the chapter to its book, and the book is what access is checked against. A refusal is a 404, indistinguishable from a chapter that never existed, because telling a stranger which IDs are real and merely forbidden is an invitation to enumerate the library. The stored file path is re-checked against the uploads directory too: a path from the database is only as trustworthy as whatever wrote it.
- **2026-09-05** — Twenty-seven checks pass against a real 80,044-byte WAV, comparing returned bytes against the file on disk slice by slice: full 200, `bytes=0-99`, an open-ended seek at 40000, a `-50` suffix, a 416 with `bytes */80044` for a range past the end, a bodyless `HEAD`, a 304 for the current ETag, and a stale `If-Range` correctly falling back to the whole file. Four refusals: an unknown chapter, a book ID passed off as a chapter, and a chapter of a draft book while logged out — all 404, no bytes.
- **2026-09-05** — The range arithmetic lives in `Support\ByteRange`, not in the player. A range over a byte count knows nothing about audiobooks, so by the rule in `CLAUDE.md` it belongs to the extractable scaffolding — and it is the part CI can actually test. 23 more unit tests, including a property that every answer is a slice that exists inside the file. `Stream` keeps only the parts that need WordPress.
- **2026-09-05** — **DECISIÓN:** the endpoint is a query variable, `?volumina_audio={id}`, not a rewrite rule. A rewrite needs flushing, and a flush that does not happen is a plugin that looks broken on activation. The URL is generated in exactly one place, `Stream::url()`, which is the single seam S4 needs for `volumina_chapter_audio_url` when Pro substitutes signed URLs. Pretty audio URLs are in `docs/backlog.md`.
- **2026-09-05** — Testing note worth keeping. PHP's built-in server on this machine serves exactly one request and then stops accepting — it fails the same way on `/`, which is plain WordPress, so it is the environment and not the plugin. Two causes were found and one remains: a background process whose stdout is a pipe nobody drains will block, so the server is launched with its output redirected to a file; beyond that it still wedges after one request. The endpoint is therefore driven through PHP's CGI SAPI (`php-cgi.exe`) instead, which gives real headers, real status codes and real bytes with no server at all. That harness is better evidence than curl was going to be.
- **2026-09-05** — S2.2 to S2.10 done and verified: the player. Chapter navigation from the transport and from the chapter list, a position kept per listener, playback speed, 15- and 30-second skips, a sleep timer with minute presets and an end-of-chapter option, full keyboard control, and a screen-reader account of everything that changes. The page arrives with a working native `<audio controls>`; the script removes the controls only once it has got far enough to replace them, so a listener whose JavaScript failed gets a plain player rather than a dead one.
- **2026-09-05** — Thirty-four checks pass in Chromium against a real two-minute WAV per chapter, three runs in a row: audio genuinely advances, pause pauses, back-15 from three seconds lands at zero rather than below it, forward-30 never runs past the end, next and previous move and refuse to move past the ends, the chapter list is the chapter navigation and marks the current chapter with `aria-current`, speed reaches the audio element and is announced, the end-of-chapter sleep timer stops instead of advancing and resets itself, `n` and `p` change chapter from the keyboard, all eight controls take focus, and a reload comes back to the right chapter at the right second. Zero axe violations against WCAG 2.1 AA at 390px and 1280px, no sideways scroll, and every touch target at least 44px.
- **2026-09-05** — The signed-in path proved separately: a position POSTs to `volumina/v1/progress/{book}` and reads back, a chapter belonging to another book is refused with 400, a request with no nonce is refused with 401, and a fresh visit with the browser's own storage cleared still resumes from the account. **DECISIÓN:** a guest's position stays in their own browser. Storing it server side would mean inventing an identifier for someone who never asked to be identified, and a listening position is not worth a tracking cookie. See `docs/decisions.md`.
- **2026-09-05** — Three failures the first run was hiding were all the test's fault, not the player's: five-second chapters were ending while the test looked elsewhere, and the signed-in checks were racing the player's own autosave, which was busy doing exactly what it should. Two-minute chapters and a paused element before each assertion; three clean runs since. Worth remembering: when a test and the product disagree about timing, the product is usually right.
- **2026-09-05** — Also worth remembering: the JS error on the signed-in page comes from WordPress core's own `wp-admin/js/user-profile.js` on the front end, not from the plugin — the whole stack is core and jQuery. The test now counts only errors raised from the plugin's own scripts, and says so when it ignores one. `debug.log` appeared once, holding a single `wp_update_themes()` TLS warning from core failing to reach WordPress.org on this machine. Neither is ours.
- **2026-09-05** — Testing infrastructure, since PHP's built-in server is unusable here: a small Node CGI server (`php-cgi` per request) now backs the browser tests. Real concurrency, real range requests, real cookies, real POST bodies — a browser cannot tell it from a host. It lives in the scratch directory, not in the repository. `.pot` regenerated: 161 strings.
- **2026-09-05** — S2 slice verification. A real eighteen-chapter, nine-hour audiobook was listened to across four separate browser sessions on an emulated Pixel 7, each one a fresh context carrying nothing but a login cookie — no memory, no `localStorage`, exactly what a second evening on a phone looks like. It resumed at the right chapter and the right second every time: chapter 3 at 20:34, chapter 11 at 25:00, then a chapter played out to its end and rolled into chapter 12, and the next session came back to chapter 12 at 42 seconds. A guest doing the same within one browser was remembered too. Zero axe violations in Twenty Twenty-Five and Twenty Twenty-One, phone and desktop.
- **2026-09-05** — The nine hours are real. One 30-minute WAV on disk serves all eighteen chapters: each chapter keeps its own ID, its own stream URL and its own position, which is everything the player and the progress table work with, and it saves 259 MB of identical bytes. The book reports 9:00:00 on the front end because the chapters add up to it.
- **2026-09-05** — Two real defects came out of that run, both invisible to every linter. A seek was only saved when it came from the slider, so a position moved from a phone's lock screen, a keyboard shortcut or the skip buttons could be lost; the player now saves on the audio element's own `seeked` event, whatever caused it. And a play attempt aborted by loading the next chapter was being announced to screen-reader users as "Ready to carry on where you left off", which is a message about autoplay being blocked and was simply untrue; only a `NotAllowedError` says that now.
- **2026-09-05** — Dragging the seek bar no longer moves the audio on every pixel. `input` moves the labels, `change` moves the audio. Scrubbing a nine-hour book should not make the decoder and the network chase the pointer, and it makes `seeked` mean what it says: the listener has finished choosing.
- **2026-09-05** — **S2 is complete except for one thing, and it is worth being exact about which.** "Mobile layout verified on a real phone" is not done, because there is no physical device here. Pixel 7 emulation with touch and a mobile user agent, 44px touch targets measured rather than assumed, and a four-session listening run are a good proxy; they are not a phone. It stays unchecked in `docs/STATE.md` and must happen on real hardware before S6.
- **2026-09-05** — Two more environment notes, neither of them the plugin. `debug.log` appeared twice during this work: once holding a `wp_update_themes()` TLS warning from core failing to reach WordPress.org, once holding a failed `copy()` from a fixture script of mine with a mangled path. Both cleared; the log is empty again. And Twenty Twenty-One's own select styling paints a background image, so axe reports the speed and sleep controls as "needs review" for contrast in that theme — the colours are the theme's, since this stylesheet declares none.
- **2026-09-05** — Session resumed after a power cut. Work sat uncommitted and unlogged: the S3 groundwork. Recovered, verified and committed rather than redone. `Frontend\Audiobook` now holds the rendering that lived in `Frontend\BookContent`, `templates/chapters.php` was split out of `templates/book.php`, and `Support\RenderOnce` keeps the content filter and a block from both rendering the same book. Three PHPCS violations in the recovered files were fixed (docblock alignment, and an unprefixed `$durations` in template scope, now `$volumina_durations`). `RenderOnceTest` added — 48 tests, 106 assertions, green. `composer lint` and `npm run lint` clean, PHPStan level 6 clean, `.pot` regenerated at 161 strings, unchanged, because the strings moved files rather than multiplied.
- **2026-09-05** — **DECISIÓN:** the S3 blocks render through the same `Frontend\Audiobook` as the front end, and `BookContent` stays. See `docs/decisions.md`. That closes the question `docs/STATE.md` was holding open at the start of S3.
- **2026-09-05** — Runtime check of the recovered refactor against the local WordPress: book 26 renders 12,232 bytes with the player, the details list and all eighteen chapter names; `chapter_list()` on its own gives eighteen names with times, and without times when asked; `render()` honours `player`, `cover` and `heading` being switched off; `RenderOnce` claims once and refuses the second caller. `debug.log` absent.
